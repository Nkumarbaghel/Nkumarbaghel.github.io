document.addEventListener("DOMContentLoaded",()=>{const nav=document.querySelector("#nav"),menu=document.querySelector("#menu");menu?.addEventListener("click",()=>nav.classList.toggle("open"));const box=document.querySelector("#posts"),detail=document.querySelector("#detail");if(!box)return;const search=document.querySelector("#search"),cat=document.querySelector("#cat");const date=s=>new Date(s+"T00:00:00").toLocaleDateString("hi-IN",{day:"numeric",month:"long",year:"numeric"});const esc=s=>String(s).replace(/[&<>"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",""":"&quot;"}[c]));posts.forEach(p=>cat.insertAdjacentHTML("beforeend",`<option value="${esc(p.category)}">${esc(p.category)}</option>`));function card(p){return `<article class="card"><small>${esc(p.category)} • ${date(p.date)}</small><h2>${esc(p.title)}</h2><p>${esc(p.excerpt)}</p><a href="blog.html?post=${encodeURIComponent(p.id)}">पूरा लेख पढ़ें →</a></article>`}function render(){let q=(search.value||"").toLowerCase(),c=cat.value;box.innerHTML=posts.filter(p=>(c==="all"||p.category===c)&&(!q||(p.title+p.excerpt).toLowerCase().includes(q))).map(card).join("")||"<div class='card'>कोई लेख नहीं मिला।</div>"}search.addEventListener("input",render);cat.addEventListener("change",render);const id=new URLSearchParams(location.search).get("post");if(id){const p=posts.find(x=>x.id===id);if(p){box.style.display="none";document.querySelector(".tools").style.display="none";detail.innerHTML=`<a href="blog.html">← सभी लेख</a><small>${esc(p.category)} • ${date(p.date)}</small><h1>${esc(p.title)}</h1><div class="article">${p.content}</div>`}else detail.style.display="none"}else{detail.style.display="none";render()}});